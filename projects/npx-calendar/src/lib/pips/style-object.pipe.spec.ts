import { StyleObjectPipe } from './style-object.pipe';

describe('StyleObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
